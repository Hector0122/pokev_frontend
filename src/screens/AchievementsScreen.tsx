import React from 'react';
import PlaceholderScreen from '../components/PlaceholderScreen';

// Logros (§15) — hitos que se celebran, nunca lo que falta. El catálogo
// completo con estado de desbloqueo (mismo diseño que
// pokev_backend AchievementsService) se conecta en la siguiente change.
export default function AchievementsScreen() {
  return (
    <PlaceholderScreen
      icon="🏆"
      title="Logros"
      description="Cada hito que ya conseguimos juntos, papá e hijo."
    />
  );
}
