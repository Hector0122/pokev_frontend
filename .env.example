# Key compartida que el backend exige en cada request (header x-app-key,
# ver ApiKeyGuard en pokev_backend) — mismo valor que APP_API_KEY en el
# .env de pokev_backend. Sin esto, cualquier llamado a la API falla con 401.
APP_API_KEY=
