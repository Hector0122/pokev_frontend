<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Sin permiso CAMERA a propósito: react-native-image-picker delega en
         la app de cámara del sistema (intent implícito) para el escaneo de
         cartas — si declarásemos el permiso acá, la librería exige pedirlo
         en runtime nosotros mismos; sin declararlo, "no permissions
         required" según su propia doc. -->


    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- Widget de cartas favoritas — ver src/services/favoritesWidget.ts. -->
      <receiver
        android:name=".widget.FavoriteCard"
        android:exported="false"
        android:label="Carta favorita">
        <intent-filter>
            <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            <action android:name="com.pokev.WIDGET_CLICK" />
        </intent-filter>
        <meta-data
            android:name="android.appwidget.provider"
            android:resource="@xml/favorite_card_widget_info" />
      </receiver>
    </application>
</manifest>
